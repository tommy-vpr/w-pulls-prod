// Remove "use client" from this file and remove all the component code
import CartClient from "./(components)/CartClient";

export const metadata = {
  title: "Cart",
};

export default function CartPage() {
  return <CartClient />;
}
